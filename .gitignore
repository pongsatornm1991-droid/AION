.env
config.env
__pycache__/
*.pyc
.DS_Store
link_memory_to_onedrive.bat
memory_local_backup/
.obsidian/
_patch_*.py*

# memory/ is symlinked to a personal OneDrive path on this machine
# (see aion.md); tracking it in git would commit a machine-specific
# absolute path that breaks on any other machine or clone.
memory
AION Brain Vault/

# Local clone of the private aion-memory-data repo, kept fresh by
# tools/sync_memory_from_github.py so the Observatory dashboard can
# show real production activity. Machine-specific, never tracked.
aion-memory-data-sync/
.env.memory_sync
client_secret.json
