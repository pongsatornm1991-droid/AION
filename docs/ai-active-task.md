# Active AI work claim

This is a small, overwrite-in-place coordination board for Codex and Claude.
It prevents concurrent sessions from editing the same release path. It is not
a historical log: replace this block when taking a task, and set status to
`clear` when handing off. A claim expires at its stated time so a crashed
session never blocks the company indefinitely.

Status: clear
Owner: Claude
Started: 2026-09-21 18:02 Asia/Bangkok
Lease expires: n/a
Scope: RESOLVED -- owner asked "what's next / what's the weak point"; investigated the real, currently-failing GitHub Actions runs (via device shell + the owner's real signed-in browser to read job logs, since this sandbox cannot see repo secrets or full logs itself) instead of guessing. Found and root-caused why `AION - subject-first scene production` (the workflow that builds the Thu-Sun Shorts release buffer, Mon/Tue/Wed cron) has been hard-failing: `CreatorSceneProduction._cover_exists()` (brain/creator_scene_production.py) only accepted a landscape 16:9 cover, while `YouTubeCreatorQueue._cover_quality()` (the check that actually gates release) has long accepted a vertical 9:16 cover for Shorts. Venus's real, correct 1080x1920 cover therefore always read as "missing" here, so `_episode()` kept re-selecting the already-published Venus every shift; `produce_once()` found no scenes left to render (made=[]) and, seeing cover_path.is_file() was already true, never even attempted to fix the cover -- it returned "scene-generation-unavailable", which `produce_ready_episodes()` treats as a reason to stop the whole shift after just one report, and `produce_creator_scenes.py --require-no-failures` treats as a hard CI failure. Net effect: every scheduled shift died on Venus before it could ever reach a real new storyboard, which is why the buffer never moved past 1/4. Fixed by making `_cover_exists()` and `_cover_prompt()` format-aware (mirrors `_cover_quality()` exactly: vertical 9:16 OR widescreen 16:9 both valid, short-format prompts now ask for a vertical asset) and by making `produce_once()`'s cover-generation guard call `_cover_exists()` instead of the weaker `cover_path.is_file()`, so a genuinely wrong/missing cover is actually regenerated instead of silently skipped. Added a regression test encoding this exact real scenario. Confirmed against the REAL repo data (not just tests) that `_episode('illustrated-narrated-short')` now correctly returns None instead of re-selecting Venus. Ran the full test suite in 4 chunks (907+ tests): all green except 3 pre-existing failures, all caused by this device shell's own inability to traverse the `memory` symlink (confirmed unrelated -- none reference this module). IMPORTANT CAVEAT for whoever picks this up: this fix removes the blocking bug but does NOT by itself create new Shorts -- `_episode('illustrated-narrated-short')` against real content currently returns None, meaning there is genuinely no new short-format storyboard staged right now. Story/Studio still needs to draft and stage new storyboards upstream of this workflow for the buffer to actually reach 4/4. Also flagged, not yet touched: `.github/workflows/reel-cycle.yml` has a duplicate `OPENAI_API_KEY` env key (lines 45 and 53) that hard-fails it at config-parse time -- but it is `workflow_dispatch`-only (a manual legacy repair tool per its own header comment), so it is not blocking anything automatic; and `AION - Publish public brain summary` failed today with exit code 128 (looks like the same known bare-git-push-race class flagged in the 2026-09-20 session log, not yet fixed there either).
Files: `brain/creator_scene_production.py`, `tests/test_creator_scene_production.py`, `docs/ai-active-task.md`, `docs/ai-session-log.md`.
Handoff: The next assistant must read `AGENTS.md`, the last 10 session-log entries, and this board before working.
