# Active AI work claim

This is a small, overwrite-in-place coordination board for Codex and Claude.
It prevents concurrent sessions from editing the same release path. It is not
a historical log: replace this block when taking a task, and set status to
`clear` when handing off. A claim expires at its stated time so a crashed
session never blocks the company indefinitely.

Status: clear
Owner: none
Started: n/a
Lease expires: n/a
Scope: none
Handoff: Codex repaired exhausted-question queue starvation in 95a8a66 and
manually started learning workflow run #144 (35859954982). It is queued behind
the shared memory-data lock; verify its result before operating the Creator
queue, research-to-story, scene production, assembly, or today's YouTube
release path.
