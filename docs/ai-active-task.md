# Active AI work claim

This is a small, overwrite-in-place coordination board for Codex and Claude.
It prevents concurrent sessions from editing the same release path. It is not
a historical log: replace this block when taking a task, and set status to
`clear` when handing off. A claim expires at its stated time so a crashed
session never blocks the company indefinitely.

Status: clear
Owner: Claude
Started: 2026-09-21 18:31 Asia/Bangkok
Lease expires: n/a
Scope: RESOLVED -- owner asked to do both of this session's own follow-up proposals to the best possible quality: (a) an automated YouTube-vs-Studio-memory drift detector, and (b) deduplicating the git-push-retry idiom into one shared composite Action. (a) is done in full: `tools/check_youtube_publication_drift.py` (new) compares the real YouTube channel's uploaded-video list against `YouTubeCreatorQueue`'s recorded episodes and flags any video with no matching memory record; `.github/workflows/youtube-publication-drift.yml` (new) runs it daily at 08:40 Bangkok plus after every Creator Studio run, posts a Telegram alert on drift, and is wired into automation-health.yml's watch list (58/58 now). 5 new tests all pass. Detect-and-alert only, by design -- it never auto-fixes, consistent with this repo's require-human-confirmation rule for episode-to-video matching. (b) is done for the safe subset only, on purpose, not for all 51 files: built `.github/actions/commit-and-push/action.yml`, verified correct with a from-scratch local git-race test harness (four tests: no-op detection, mkdir-on-dirname vs mkdir-on-directory for trailing-slash paths, a REAL two-clone non-fast-forward push correctly recovered by the retry chain) since this sandbox cannot execute real GitHub Actions runs. Surveyed all 51 files' commit-and-push step bodies programmatically and found 53 total occurrences (2 files have two each); of those, 25 occurrences across 25 files are a byte-for-byte match of the plain idiom (git config + optional mkdir + git add + if-diff-then-commit-push-retry-fi, nothing else) and were migrated to `uses: ./.github/actions/commit-and-push`, each verified to still parse as valid YAML afterward (58/58 workflow files + the new action all parse clean). The other 28 occurrences were deliberately left untouched because they are not byte-identical to the idiom: most add an unconditional `git pull --rebase origin main` before the push-retry block (a real behavioral extra, not idiom noise), several use an if/else with their own custom "nothing to commit" message instead of the action's generic one, and `reel-cycle.yml`'s first block also sets two `$GITHUB_ENV` flags after the commit block that a later step in the same job depends on. Migrating those safely would mean either silently dropping real behavior or splitting each into two steps by hand per file -- neither is safe to do mechanically with no way to run real CI from here, so they were left on their proven-working inline form rather than risk a live regression in a system that posts autonomously to real YouTube/Instagram/Facebook accounts. `obsidian-brain.yml` (space in path, breaks the action's space-separated `paths` design) and `growth-pulse.yml` (different git identity, no if/fi wrapper) were excluded from the survey entirely, as decided earlier this session.
Files: `tools/check_youtube_publication_drift.py`, `tests/test_check_youtube_publication_drift.py`, `.github/workflows/youtube-publication-drift.yml`, `.github/workflows/automation-health.yml`, `tools/youtube.py`, `.github/actions/commit-and-push/action.yml`, and 25 migrated workflow files: `asset-hygiene.yml`, `autonomic-drive.yml`, `autonomous-inquiry.yml`, `content-experiment-evaluator.yml`, `costume-briefs.yml`, `creator-autonomy.yml`, `creator-reference-study.yml`, `crosspost-latest-reel.yml`, `evolution-cycle.yml`, `experiment-runner.yml`, `improvement-approvals.yml`, `improvement-review.yml`, `platform-recovery.yml`, `reel-cycle.yml` (its second block only), `render-creator-library.yml`, `research-to-story.yml` (its first block only), `revenue-brain.yml`, `scientific-discovery.yml`, `story-episode-002.yml`, `story-episode-003.yml`, `story-episode-004.yml`, `story-episode.yml`, `youtube-audience.yml`, `youtube-learning.yml`, `youtube-shorts.yml`.
Handoff: The next assistant must read `AGENTS.md`, the last 10 session-log entries, and this board before working. Not yet pushed to origin as of this write -- confirm `git push origin main` has actually run before assuming any of this is live. Once the owner has watched a few real scheduled runs of the 25 migrated workflows (or manually triggered one via `workflow_dispatch`) and confirmed the composite action behaves identically in practice, the remaining 28 occurrences could be migrated too -- but each one now needs a human (or a session with real CI access) to decide, per file, whether to drop the extra `git pull`, keep a custom no-op message via a small action tweak, or split reel-cycle.yml's step in two. Do not attempt that as a blind mechanical pass like this one was; this one only succeeded because it was scoped down to byte-identical cases.
